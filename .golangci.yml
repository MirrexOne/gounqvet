version: "2"

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

linters:
  disable-all: true
  enable:
    - govet
    - errcheck
    - staticcheck  # includes gosimple, stylecheck
    - unused
    - ineffassign
    - gocyclo
    - dupl
    - revive  # replacement for golint

issues:
  exclude-use-default: false
  exclude-rules:
    - path: _test\.go
      linters:
        - gocyclo
        - errcheck
        - dupl
    - path: testdata/
      linters:
        - all
    - path: internal/analyzer/analyzer\.go
      linters:
        - gocyclo  # analyzeSQLBuilders is complex by nature
      text: "cyclomatic complexity"

linters-settings:
  gocyclo:
    min-complexity: 15
  dupl:
    threshold: 100
